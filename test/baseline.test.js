import test from 'node:test'; import assert from 'node:assert/strict'; import { readFile } from 'node:fs/promises'; import { ManifestStore } from '../src/manifest-store.js'; import { HandoverService } from '../src/handover-service.js'; import { manifestView } from '../src/audit-view.js';
test('单分段联单可以完成接收',async()=>{const item=JSON.parse(await readFile(new URL('../fixtures/manifest.json',import.meta.url)));const store=new ManifestStore();store.create(item);const service=new HandoverService(store);assert.equal(service.accept('M-100','S-1',805,'sig-demo').status,'accepted');const view=manifestView(store.get('M-100'));assert.equal(view.state,'completed');assert.equal(view.acceptedKg,805);});
